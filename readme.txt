Nav bar:
Both box shadows and rounded borders are used to create the styling for the nav bar and dropdown menu.

Additional features:
Gradients and background images are used to style the bar below the header.

Transitions were used to smooth the highlighting of the nav bar buttons.

Some features require IE11 or newer to use; all other browsers should be supported.